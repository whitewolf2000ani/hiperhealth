name: sdx
channels:
  - conda-forge
  - nodefaults

dependencies:
  - python >=3.10,<=3.13
  - pip
  - poetry
  - nodejs  # used by semantic-release
  - tesseract
  - libmagic
  - pip:
      - paginate==0.5.6  # distlib issue
